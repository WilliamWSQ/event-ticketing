/**
 * The concert catalogue now lives in the backend (GET /api/concerts) and is
 * fetched into BookingContext at startup. Only client-side config remains here.
 */

/** The featured concert opened from the home hero CTA. */
export const FEATURED_ID = 'nova';
